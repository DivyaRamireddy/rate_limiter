# Rate Limiter

A simple Python rate limiter that restricts how many requests a user can make within a fixed time window.

---

## Features
- Limits requests per user
- Uses time-window logic
- Prevents brute-force or abuse
- CLI-based simulation

---

## How to Run
```bash
python rate_limiter.py# rate_limiter
